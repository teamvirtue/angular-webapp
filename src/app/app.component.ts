import { Component, ViewChild } from '@angular/core';
import * as $ from 'jquery';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent {
  /*isDisabled: boolean = true;
  isDarkTheme: boolean = false;
  isPrimaryColor: boolean = true;
  isSecondaryColor: boolean = false;
  eventTabIndex: number = 0;

  @ViewChild('homeTabBar') homeTabs: any;
  @ViewChild('homeScrollFrame') homeScrollFrame: any;

  switchToTab() {
    this.homeTabs.setTabActiveAtIndex(1);
  }

  scrollForward() {
    this.homeScrollFrame.scrollForward();
  }

  scrollBack() {
    this.homeScrollFrame.scrollBack();
  }

  toggleDisabled() {
    this.isDisabled = !this.isDisabled;
  }

  handleTabChange(event: { index: number, tab: any }) {
    this.eventTabIndex = event.index;
    console.log(event);
  }*/
}

